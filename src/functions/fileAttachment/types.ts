export interface UserDocument {
    fileId: string
    gigSeekerId: string
    fileName: string
    filePath: string
}

export interface GigSeekerId {
    gigSeekerId: string
}

export interface GigSeekerIdFileName {
    gigSeekerId: string
    fileName: string
}