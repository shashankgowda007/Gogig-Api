export interface Bookmarks {
    bookmarkId: string,
    gigId: string,
    userId: string
}

export interface GigSeekerId {
    userId: string
}

export interface BookmarkId {
    bookmarkId: string
}